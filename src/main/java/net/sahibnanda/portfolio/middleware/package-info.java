/**
 * Resolves a per-visitor session id from the {@code X-Session-Id} header,
 * generating and setting one when absent, so {@code Core} never needs raw
 * servlet types.
 */
package net.sahibnanda.portfolio.middleware;
